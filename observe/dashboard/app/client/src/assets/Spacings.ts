export class Spacings {
  /**
   * For pages that have a left/right split (tables with a fixed column, for example),
   * this is the width that they should conform to
   */
  static leftColumnWidth: 300;

  static pageLeftPadding: 10;

  static pageLeftPaddingLarge: 50;

  static tabContentHeaderHeight: 84;
}
