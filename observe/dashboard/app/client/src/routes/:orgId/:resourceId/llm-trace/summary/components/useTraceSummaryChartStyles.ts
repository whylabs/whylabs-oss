import { createStyles } from '@mantine/core';

export const useTraceSummaryChartStyles = createStyles(() => ({
  card: {
    height: 275,
    minHeight: 275,
    position: 'relative',
  },
  cardTitleContainer: {
    marginBottom: 0,
  },
}));
