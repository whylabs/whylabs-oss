export const Spacings = {
  /**
   * For pages that have a left/right split (tables with a fixed column, for example),
   * this is the width that they should conform to
   */
  leftColumnWidth: 300,
  pageLeftPadding: 10,
  pageLeftPaddingLarge: 50,
  tabContentHeaderHeight: 84,
};
