import { createStyles } from '@mantine/core';

export const useUtilityStyles = createStyles({
  invisibleButton: {
    background: 'transparent',
    border: 'none',
    padding: 0,
    margin: 0,
    cursor: 'pointer',
    outline: 'none',
  },
});
